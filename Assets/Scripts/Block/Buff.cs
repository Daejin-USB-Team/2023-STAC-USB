using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Buff : MonoBehaviour
{
    //문제 결과 값 확인
    //만약 버프 값이 맞다면 씬 전환 카운트 증가하기
    //버프 추가하기
    //너프 추가하기
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
